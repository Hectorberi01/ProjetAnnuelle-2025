
services:
  promotions:
    build: .
    container_name: projects-service
    restart: always
    ports:
      - "3002:3002"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    #external: true