
services:
  auth:
    build: .
    container_name: service-auths
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    external: true