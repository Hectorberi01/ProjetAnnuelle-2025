
services:
  auth:
    build: .
    container_name: service-auths
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    external: true