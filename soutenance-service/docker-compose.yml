
services:
  soutenance:
    build: .
    container_name: service-soutenance
    restart: always
    ports:
      - "3010:3010"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    external: true