
services:
  groups:
    build: .
    container_name: service-groups
    restart: always
    ports:
      - "3004:3004"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    external: true