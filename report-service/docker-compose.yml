
services:
  promotions:
    build: .
    container_name: report-service
    restart: always
    ports:
      - "300§:300§"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    external: true