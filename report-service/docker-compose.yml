
services:
  report:
    build: .
    container_name: service-report
    restart: always
    ports:
      - "3005:3005"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    external: true