
services:
  gateway:
    build: .
    container_name: gateway_api
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    #external: true